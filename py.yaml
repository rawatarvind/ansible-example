---
 - name: Create a tar.gz archive of a single file
   hosts: all
   task:

    - archive:

           path: /home/sarvind/helloword.txt
           dest: /opt/king/helloword.txt.tar.gz
           format: gz
           force_archive: true
